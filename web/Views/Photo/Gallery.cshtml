@model IEnumerable<Web.Models.Image>

<div class="gallery">
    <div class="container-fluid">
        <div class="row">
            @foreach (var image in Model)
            {
                @Html.Partial("~/Views/Photo/Thumbnail", image)
            }
        </div>
    </div>
</div>