@model IEnumerable<Web.Models.Page>
@{ ViewBag.Summarize = Model.First().Category == "Blog"; }
@foreach (var page in Model)
{
    @Html.Partial("SubPage", page)
    if (page != Model.Last())
    {
        <hr />
    }
}